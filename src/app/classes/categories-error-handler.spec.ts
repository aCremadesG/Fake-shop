import { CategoriesErrorHandler } from './categories-error-handler';

describe('CategoriesErrorHandler', () => {
  it('should create an instance', () => {
    expect(new CategoriesErrorHandler()).toBeTruthy();
  });
});
