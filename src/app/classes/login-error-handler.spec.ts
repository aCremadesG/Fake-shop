import { LoginErrorHandler } from './login-error-handler';

describe('LoginErrorHandler', () => {
  it('should create an instance', () => {
    expect(new LoginErrorHandler()).toBeTruthy();
  });
});
