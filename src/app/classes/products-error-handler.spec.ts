import { ProductsErrorHandler } from './products-error-handler';

describe('ProductsErrorHandler', () => {
  it('should create an instance', () => {
    expect(new ProductsErrorHandler()).toBeTruthy();
  });
});
